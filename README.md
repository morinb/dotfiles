# Dotfiles

My macOS development environment setup.

## 🚀 Install

```bash
git clone git@github.com:morinb/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
bash bootstrap.sh
```
or directly with curl

    bash <(curl -fsSL https://raw.githubusercontent.com/TON_USER/dotfiles/main/install.sh)
